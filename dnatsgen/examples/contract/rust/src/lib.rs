// Hand-written (NOT generated): declares the generated modules for the crate.
// `dnats` is the once-per-crate runtime; `contract` is this proto's messages +
// NATS clients/servers/publishers/subscribers.
pub mod contract;
pub mod dnats;
